% =====================================================================
%  Wrapper para gerar a versão em PORTUGUÊS (secundária).
%  No Overleaf: defina este arquivo como "Main document" e compile.
%  Linha de comando: xelatex main-pt.tex
%  Toda a edição de conteúdo é feita em main.tex (fonte única).
% =====================================================================
\def\CVPT{}\input{main.tex}
